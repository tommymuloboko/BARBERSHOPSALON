# Set this in Vercel → Project Settings → Environment Variables
# (and locally, copy this file to .env.local — leave unset to use the dev proxy)
VITE_API_BASE_URL=https://your-backend.onrender.com/api
